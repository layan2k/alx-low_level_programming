#include <stdio.h>

/**
 * main - Prints numbers between 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int index =48;

	while (index < 58)
	{
		putchar(index);
         index++;
	}
	putchar('\n');
	return (0);
}
