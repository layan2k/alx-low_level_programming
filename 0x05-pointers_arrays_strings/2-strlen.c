#include "holberton.h"
#include <string.h>

/**
 *int _strlen - returns the length of a string.
 *@s: been the string
 * Return: length of string.
 */
int _strlen(char *s){
	int summer;
	*s = summer;
   int length = strlen(summer);
   printf("%d\n", length);
}
