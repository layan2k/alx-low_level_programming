#include <stdio.h>
#include <stdlib.h>

/**
 * main - Prints the numbers 1-10.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int index = 0;
	while (index < 11)
	{
		printf("%d/", index);
		index ++;
	}
	return (0);
}
