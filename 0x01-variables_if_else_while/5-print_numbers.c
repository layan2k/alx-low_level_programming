#include <stdio.h>
#include <stdlib.h>

/**
 * main - Prints the numbers 1-10.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (int index = 0; index < 10; index++)
	{
		printf("%d", index);	
	}
	putchar('\n');
	return (0);
}
