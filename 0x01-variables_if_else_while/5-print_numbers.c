#include <stdio.h>
#include <stdlib.h>

/**
 * main - Prints the alphabets.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int index = 0;
    while (index < 11)
    {
        printf("%d/", index);
    }
    return (0);
}
